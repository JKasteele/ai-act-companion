# Screenshots

The main `README.md` embeds three screenshots from this folder. Add them here
with these exact filenames (PNG):

| File | What to capture |
|---|---|
| `result.png` | The **Classification result** view (e.g. after "Load example" → "Classify & generate") |
| `report.png` | The rendered **report preview** (Risk assessment tab) |
| `ai-assist.png` | The **AI assist** panel (free-text box + draft flow) |

Tip: run the app (`uvicorn app.main:app --reload`), open
`http://127.0.0.1:8000`, and use your OS screenshot tool. Crop to the content
area for a clean look.
