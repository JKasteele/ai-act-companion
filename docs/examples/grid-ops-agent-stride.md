# STRIDE Threat Model - GridSentinel autonomous operations agent
_Assessment id: `grid-ops-agent` · Generated: 2026-06-17T00:00:00+00:00 · AI Act Companion v0.6_

> This report was generated by AI Act Companion as an aid for a structured self-assessment. It is NOT legal advice and does not replace an assessment by a qualified lawyer or the competent supervisory authority. Classification is based on the answers provided by the user.
Models the system across the six **STRIDE** categories (Spoofing, Tampering, Repudiation, Information disclosure, Denial of service, Elevation of privilege). Each category is driven by the security-architecture answers (section 9); the severity is the same architecture-aware rating the AI security lens computes, linked to [Art. 15](https://artificialintelligenceact.eu/article/15/).

STRIDE threat model across the six categories, with the architecture-aware severity from the AI security lens. Highest severity: **Critical**.

> This STRIDE view is a structured threat-modelling aid driven by the stated architecture. It does not replace a full threat-modelling exercise, a security review or legal advice.
>
> _STRIDE is Microsoft's threat-modelling framework. The mapping of each category to the security-architecture intake fields, to the OWASP LLM Top 10 family and to the EU AI Act articles is a Companion-derived analytical alignment, not an official crosswalk. Severity is computed deterministically from the structured arch_* fields by the AI security lens._

## Severity overview
Severity is computed deterministically from the security-architecture fields. Highest: **Critical**.

| STRIDE category | Severity | Driven by |
|---|---|---|
| **S — Spoofing** | High | weak/shared credentials are spoofable (arch_auth_strength=weak). |
| **T — Tampering** | Critical | write access AND unreviewed downstream actions (arch_api_write=yes, arch_downstream_actions=yes). Elevated: a shared service account without API-layer RBAC lets every user inherit its reach (arch_identity_model=shared-service-account). |
| **R — Repudiation** | Low | interactions are logged with user identity and bounded retention (arch_logging=yes). |
| **I — Information disclosure** | Critical | the model has write access to backend data, so a successful injection can change data (arch_api_write=yes). |
| **D — Denial of service** | High | no rate limits / quotas / cost caps (arch_rate_limits=no) — exposed to resource exhaustion. |
| **E — Elevation of privilege** | Critical | write access AND unreviewed downstream actions (arch_api_write=yes, arch_downstream_actions=yes). Elevated: a shared service account without API-layer RBAC lets every user inherit its reach (arch_identity_model=shared-service-account). |

## Per-category analysis

### S — Spoofing
Can an actor impersonate a user, or drive the system, without proving identity?

**Questions that determine the risk:**
- How do users authenticate (none / weak / strong SSO)?
- Is the system internal-only or exposed to untrusted users?
- Is user identity verified on every request?

| Architecture factor | Answer (from intake) |
|---|---|
| User authentication | Weak (shared / static credentials) |

| Aspect | Detail |
|---|---|
| Severity | **High** — weak/shared credentials are spoofable (arch_auth_strength=weak). |
| OWASP LLM Top 10 | — |
| EU AI Act | [Art. 15](https://artificialintelligenceact.eu/article/15/) |

### T — Tampering
Can data, backend state or model inputs be modified through the system?

**Questions that determine the risk:**
- Is backend access read-only, or read-write?
- Can it trigger downstream actions (email, tickets, code/SQL) without human review?
- Is retrieval (RAG) over a corpus users can modify?

| Architecture factor | Answer (from intake) |
|---|---|
| Write/modify access to backend data | Yes |
| Unreviewed downstream actions | Yes |
| RAG over a user-modifiable corpus | Yes |

| Aspect | Detail |
|---|---|
| Severity | **Critical** — write access AND unreviewed downstream actions (arch_api_write=yes, arch_downstream_actions=yes). Elevated: a shared service account without API-layer RBAC lets every user inherit its reach (arch_identity_model=shared-service-account). |
| OWASP LLM Top 10 | LLM05 |
| EU AI Act | [Art. 15](https://artificialintelligenceact.eu/article/15/) |

### R — Repudiation
Can actions be denied afterwards for lack of attributable, retained logs?

**Questions that determine the risk:**
- Are interactions logged with user identity?
- Is retention bounded?

| Architecture factor | Answer (from intake) |
|---|---|
| Identity-attributed logging with bounded retention | Yes |

| Aspect | Detail |
|---|---|
| Severity | **Low** — interactions are logged with user identity and bounded retention (arch_logging=yes). |
| OWASP LLM Top 10 | — |
| EU AI Act | [Art. 12](https://artificialintelligenceact.eu/article/12/) |

### I — Information disclosure
Can a user reach data they should not — other users' data, secrets, or the system prompt?

**Questions that determine the risk:**
- Own-user data only, or all-users / organisation-wide?
- Is access control enforced at the API/backend layer, or only in the prompt?
- Could the system prompt or retrieved context leak?

| Architecture factor | Answer (from intake) |
|---|---|
| Data the system can reach | All users' / organisation-wide data |
| Where access control is enforced | In the LLM / prompt (the model is the boundary) |
| RAG over a user-modifiable corpus | Yes |

| Aspect | Detail |
|---|---|
| Severity | **Critical** — the model has write access to backend data, so a successful injection can change data (arch_api_write=yes). |
| OWASP LLM Top 10 | LLM02 |
| EU AI Act | [Art. 10](https://artificialintelligenceact.eu/article/10/), [Art. 15](https://artificialintelligenceact.eu/article/15/) |

### D — Denial of service
Can the system be exhausted, or made unaffordable, through volume or cost?

**Questions that determine the risk:**
- Are there rate limits / quotas / cost caps?
- How business-critical is availability?

| Architecture factor | Answer (from intake) |
|---|---|
| Rate limits / quotas / cost caps | No |

| Aspect | Detail |
|---|---|
| Severity | **High** — no rate limits / quotas / cost caps (arch_rate_limits=no) — exposed to resource exhaustion. |
| OWASP LLM Top 10 | LLM10 |
| EU AI Act | [Art. 15](https://artificialintelligenceact.eu/article/15/) |

### E — Elevation of privilege
Can a user inherit more reach than intended via the system's backend identity?

**Questions that determine the risk:**
- Per-user delegated identity, or a shared service account?
- Is backend RBAC enforced independently of the model?

| Architecture factor | Answer (from intake) |
|---|---|
| Backend identity model | Shared service account |
| Where access control is enforced | In the LLM / prompt (the model is the boundary) |

| Aspect | Detail |
|---|---|
| Severity | **Critical** — write access AND unreviewed downstream actions (arch_api_write=yes, arch_downstream_actions=yes). Elevated: a shared service account without API-layer RBAC lets every user inherit its reach (arch_identity_model=shared-service-account). |
| OWASP LLM Top 10 | LLM06 |
| EU AI Act | [Art. 15](https://artificialintelligenceact.eu/article/15/) |
