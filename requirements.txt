# --- Core (phases 1-3, rule-based, NO AI) ---
fastapi>=0.110
uvicorn[standard]>=0.29
# pydantic ships with fastapi

# --- Phase 4 (optional AI layer) ---
# Only needed for the Ollama provider (local HTTP calls). The rule-based core
# and the 'manual' provider do not depend on it.
httpx>=0.27

# --- Dev / tests (or: pip install -e ".[dev]") ---
# pytest>=8.0
# ruff>=0.5
# mypy>=1.8
